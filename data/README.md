# Data directory

This directory is a documentation placeholder only. Do not commit videos,
YOLO caches, annotations, SQLite files or private datasets. Store a download
manifest, checksum list and licensing notes here if a public dataset is used.

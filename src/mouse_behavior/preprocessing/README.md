# Preprocessing boundary

Video metadata reading, cache validation and future frame normalization belong
here. The current lightweight path consumes completed Pose caches, so it does
not duplicate the upstream detector preprocessing in this package.

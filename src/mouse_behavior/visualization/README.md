# Visualization boundary

Rendering and timeline plots belong here. The lightweight analysis default is
non-rendering; renderers must consume persisted tracks/events and never alter
behavior decisions.

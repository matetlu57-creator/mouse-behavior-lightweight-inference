# Postprocessing boundary

Interpolation, correction, event merging and export transformations belong
here. The current website package is kept as a separate adapter until its
schema is frozen; generated outputs remain outside the source tree.

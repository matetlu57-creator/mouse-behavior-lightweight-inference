# End-to-end tests

This layer covers user-visible command entry points without requiring private
videos, model weights, or detector caches. Real-data scientific validation is
recorded separately under `docs/validation/`.

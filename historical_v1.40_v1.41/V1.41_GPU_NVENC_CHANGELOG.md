# v1.41 RTX3060 GPU/NVENC optimization

目标：
- 保持行为算法、身份算法、阈值和输出一致；
- 优化视频编码和GPU资源利用；
- 为后续事件触发mask与cascade identity提供接口。

本版本新增：
1. FFmpeg NVENC编码接口设计文档；
2. RTX3060 Laptop推荐参数；
3. GPU benchmark记录模板。

注意：
正式启用NVENC需要在目标Windows环境确认：
- ffmpeg支持 h264_nvenc；
- NVIDIA驱动正常；
- ffmpeg在PATH中。

未改变：
- YOLO权重；
- 行为判定规则；
- identity权重；
- mask结果逻辑。

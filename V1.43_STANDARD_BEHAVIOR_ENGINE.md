# v1.43 Standard Behavior Engine — 完整算法说明

## 1. 分层架构

```text
YOLO Pose
  -> Stable Identity
  -> MouseObservation
  -> Observation Quality
  -> Individual / Pair Kinematics
  -> Continuous Primitive Evidence
  -> Chase Role / Attack Role Inference
  -> Chase FSM + Attack FSM
  -> Independent Ethogram
  -> Four-Class Classification
```

核心规则：Feature 是测量事实，Evidence 是连续证据，State/Event 才是行为结论。

## 2. Observation Quality

每个 pair 使用：

- `pose_pair_quality`
- `identity_pair_quality`
- track state 映射

组合质量：

`Qpair = sqrt(Qpose * Qidentity)`

FSM 设有 `min_quality_to_open` 与 `min_quality_to_hold`。因此差质量帧不能单独开启事件，但可以在短时遮挡时维持已经建立的行为。

## 3. Pair Feature Vector

A→B 与 B→A 分别计算：

- center distance (cm)
- center distance / mean body length
- actor / target speed
- acceleration
- radial closing speed
- pursuit alignment
- target escape alignment
- direction similarity
- continuous behind score
- trajectory correlation
- nose-head / nose-body / nose-tail distance
- head-relative motion
- angular speed
- pose deformation energy
- cluster overlap / deficit / motion / active frames

### Radial closing speed

令 `r = C_target - C_actor`，`r_hat = r/|r|`：

`closing = (V_actor - V_target) dot r_hat`

正值表示 actor 正在缩短与 target 的距离。

### Pose deformation energy

关键点先进入以 body center 为原点、heading 为 x 轴、body length 归一化的体坐标系，再计算相邻测量帧共同有效关键点的 RMS 位移。因此整体平移、旋转和尺度变化不会被误当成扭打；内部姿态快速变化才产生高形变能量。

## 4. Continuous Chase Evidence

默认权重：

- distance 0.15
- actor speed 0.10
- target speed 0.10
- same direction 0.10
- pursuit 0.20
- escape 0.15
- behind 0.10
- trajectory 0.10

原 hard threshold 周围改用 fuzzy membership，避免 4.99 与 5.01 造成行为完全跳变。

A→B 和 B→A 分开得到 `C_ab`、`C_ba`：

`chase_role_confidence = abs(C_ab - C_ba)`

角色不够明确时不允许新开 chase。

## 5. Chase FSM

状态：

`IDLE -> APPROACH -> CHASE -> RECOVERY -> IDLE`

- enter threshold > exit threshold（hysteresis）；
- 必须连续满足 `confirm_seconds` 才确认；
- 进入后允许短 `exit_hold_seconds` 低证据恢复区；
- wall jump / invalid pair / 超 interaction radius 是 hard veto。

## 6. Attack Evidence

Attack 不再视为一种单一动力学模式，而分三条证据链。

### 6.1 Lunge / dynamic attack

证据：

- initiation：pursuit + lunge/closing + head-relative motion + acceleration
- contact：nose/body 接近
- reaction：target escape speed + escape alignment + turn + acceleration

必须经过时序因果：

`INITIATION -> CONTACT -> REACTION`

单帧三个条件碰巧同时高不等于完整因果链。

### 6.2 Grapple / fight

证据：

- sustained close contact
- repeated contact
- angular activity
- head/local motion
- pose deformation energy
- slow/medium center translation

用于识别中心不快但身体内部运动剧烈的贴身扭打。

### 6.3 Occlusion fight

证据：

- cluster attack hint
- detection deficit
- merged-like state
- overlap IoU
- cluster motion
- active duration
- recent contact/initiation context

cluster hint 本身不能单独新开 attack。

## 7. Attack FSM

状态：

`NONE -> ATTACK_PREPARE -> CONTACT -> ATTACK -> RECOVERY`

分支：

- lunge: prepare/contact/reaction 因果确认；
- grapple: sustained grapple score 达到持续时间；
- occlusion: occlusion score + 最近 physical/initiation context。

动态扑咬要求 `attack_role_confidence` 达标。grapple/occlusion 可以确认事件但保留 `actor_id=-1`，表示无法可靠确定发起者。

## 8. Behavior-specific Role

Chase role 与 Attack role 分开计算：

- Chase role：基于 chase evidence 差值；
- Attack role：基于 initiation/reaction 方向证据。

这样不会在对称扭打时因为 chase score 相近而强行指定攻击者。

## 9. Legacy Evidence Provider

旧版以下模块保留：

- strict chase
- window chase
- near recovery chase
- close follow chase
- strict attack
- impulse attack
- grapple attack
- occlusion overlap attack

在 `decision_mode=standard` 下，它们只提供受限的 evidence floor，不直接 OR 成最终行为。因此旧领域知识保留，但最终裁决统一经过质量、角色和 FSM。

## 10. Independent Ethogram

`标准行为事件_时序引擎.csv` 独立聚合 chase 和 attack。

例如：

```text
0.0-4.0 s chase
1.8-2.5 s attack
```

会得到一个完整 chase bout 和一个嵌套 attack bout；四分类层再组合成视频级追逐/攻击存在性。

## 11. Four-Class

底层先独立检测 chase 与 attack，最后组合：

- 0 = 非追逐非攻击
- 1 = 追逐非攻击
- 2 = 非追逐攻击
- 3 = 攻击性追逐

追逐和攻击不要求发生在同一帧。

## 12. 20 鼠 Interaction Graph

空间半径默认 17 cm。当前 bundled config 只标记 candidate，不裁剪 pair 计算；这样第一次科研回归不会改变 pair/negative coverage。

验证后打开 `prune_pair_computation=true` 时，仅邻近 edge 进入完整 pair feature；已有 occlusion evidence 的 pair 强制保留。

## 13. 后续机器学习兼容

整个前端 `Pair Feature Vector` 与决策层解耦。未来可在不改变 YOLO/Identity/Observation 的情况下，将 `BehaviorEvidenceComputer` 替换为 XGBoost/LightGBM/TCN 等监督模型，同时保留 FSM、事件聚合、质量控制和输出接口。
